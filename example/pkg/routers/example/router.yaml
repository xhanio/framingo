server: http # server name
prefix: /example # group prefix
handlers:
  - method: POST
    path: /helloworld
    func: Example # key of Handlers()
    middlewares:
      - authnuser
      - deflate # middleware names